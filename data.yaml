# `path` is the dataset root. Keep it portable: '.' resolves relative to this
# file's location, so the dataset works on any machine. Override per-machine by
# setting an absolute path or a $DATASET_ROOT env var if your images live elsewhere.
path: .

train: train/images
val: valid/images
test: test/images

nc: 2
names: ['fire', 'smoke']